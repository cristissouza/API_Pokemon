machine:
  ruby:
    version: 2.3.0
dependencies:
  override:
    - gem install bundler
   

test:
  override:
    - bundle install
   
    
- run:
     name: Run rspec in parallel
     command: |
     bundle exec rspec --profile 10 \
     --format RspecJunitFormatter \
   
